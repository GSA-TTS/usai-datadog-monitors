Slowed the `cronjob_failing` re-notification cadence from every 2h to once per day (`renotify_interval` 120 → 1440, all 25 orgs). A CronJob can stay broken for days (e.g. `console-data`'s fleet-wide DeadlineExceeded, GSA-TTS/usai#1094), and the 2h re-page flooded the channel with "still failing" re-triggers across every org. The initial trigger still fires immediately and the recovery notification still fires promptly; only the known-broken reminder stretches to daily.
