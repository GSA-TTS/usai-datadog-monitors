"Ingress gateway 5xx by upstream service (users vs internal polls)" widget on the per-tenant Edge & Request Health dashboard, grouping `trace.envoy.proxy.errors` by `resource_name` rather than only `http.status_code`. A flat 503 count cannot distinguish a user-facing outage from the frontend's own `/health` polling: doc showed 23 ingress 503s in one hour on 2026-08-18, split 20 against `api-beta` (internal, `UA=none`, frontend still returned 200 to users) versus 3 against legacy chat, which held the only real browser session. Same total, different severity.
