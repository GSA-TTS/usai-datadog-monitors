`frontend_upstream_api_unreachable` log monitor (`cert_monitors.tf`): pages when the chat-beta frontend cannot reach its upstream API (`service:usai-frontend "API connection failed"` > 20 in 10m, warn at 5). Closes the gap behind a 2026-08-13 stateoig outage where a wildcard cert (`*.prod.<tenant>`) did not cover `api.beta.prod.<tenant>`, so the frontend's server-side fetch failed `ERR_TLS_CERT_ALTNAME_INVALID` for ~25h — users got 502s while `/healthz` kept returning 200 and the edge synthetics (external, wrong hosts, gated off) could not see it.
