**Both Azure OpenAI widgets on the Model Backend Health dashboard were rendering empty, in all 25 orgs.** They queried `service:api env:production "Too Many Requests"` and `service:api env:production "Stream aborted mid-flight"`; measured against the live gsa logs API, **both return zero events over 30 days**, so both graphs had been blank for however long the service topology has been as it is. Two independent faults, both found by probing rather than reasoning. **(1) Wrong service scope.** Azure OpenAI work is split across *two* services now, `api` and `api-beta`, and the throttle signal appears under **both** — measured over 14 days in gsa: `api` = 2801, `api-beta` = 2609, and 5410 via `service:(api OR api-beta)`. Neither service alone captures it, so the `OR` form is not defensive padding, it is required for a correct count. **(2) Wrong phrase.** The log text is not "Too Many Requests" — that string appears only in `cloudtrail` logs in these orgs, which is why a bare search for it looked like it matched something. Azure's actual wording is e.g. `Your requests to gpt-5.5 for gpt-5.5-latest-guardrails-defaultv2 in eastus2 have exceeded rate limit.`, so the correct match is `"exceeded rate limit"` — **4509 events in gsa over 7 days**, happening constantly with nothing displaying it. The "Stream aborted mid-flight" widget was worse: that phrase returns 0 across **all** services and all of `env:production` over 30 days, and so do `"stream aborted"`, `"aborted mid-flight"` and bare `"abort"`. The signal it was built for (the 2026-06-02 incident) no longer exists in the logs at all, so repointing it at a variant would only have produced a differently-empty graph; it is repurposed to the upstream-500 signal that **does** exist (78 events/7d in gsa, e.g. `500: Internal Server Error | headers: {...'Server': 'envoy'...}`), which is the closest real measure of Azure-side failures reaching users. Widget titles now name both services so the scope is visible on the dashboard rather than hidden in the query. **Deliberately NOT fixed in this PR: the same two broken queries are still live in `main.tf`'s two Azure monitors**, where `on_missing_data = "default"` has kept them permanently `OK` while matching nothing — a silent-monitor bug of exactly the kind `cert_monitors.tf` documents for `synthetics.ssl.days_left`, and the reason Azure throttling has never once alerted despite 4509 events in a week. Correcting those queries while keeping `> 3 in 5m` would page roughly **19×/day per org, ~480/day across 25 orgs**: the measured 5m-bucket distribution in gsa over 7d is throttling present in 12.1% of windows, median 4, p95 76, p99 206, max 240 — i.e. throttling is a normal operating condition here, not an incident. And a count threshold cannot work fleet-wide at all, because 7d totals are gsa 4509, doc 323, ftc 15, dot 5, hud 0 — a ~900× spread, where a threshold quiet enough for gsa would never fire for dot even during a total outage. That is precisely what the `CLAUDE.md` "prefer RATES over absolute counts across tenants with different traffic" convention exists for, and the shape that produced the ~200-alert flood in PR #42. So the monitor fix needs a threshold *design* decision (ratio vs sustained-duration vs per-tenant variable) rather than a string edit, and is documented in-file with the measurements instead of being silently half-applied. Plan: 25 dashboard updates, 0 added, 0 destroyed.
